"""fuaran — a held name, deliberately empty.

The Fuaran UI wire-format host for Python is the ``fuaran-ui`` distribution, which
imports as ``fuaran_ui``.  This package exports nothing and never will: the bare
name is held so it cannot be claimed by an unrelated project and mistaken for that
host, on the posture the unscoped npm name ``fuaran`` and the bare ``fuaran`` crate
already take.

``fuaran`` is the FAMILY name.  Naming a host after it would make the UI domain
look like the whole of Fuaran, which is the confusion this placeholder exists to
prevent.
"""

from __future__ import annotations

__all__: list[str] = []
__version__ = "0.0.0"
