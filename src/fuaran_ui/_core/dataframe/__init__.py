"""The Core ``DataFrame`` twin: the columnar model, its codec and its evaluator.

Private — the published import path is :mod:`fuaran_ui.dataframe`, which re-exports
these modules unchanged. See :mod:`fuaran_ui._core` for the boundary rule.
"""
