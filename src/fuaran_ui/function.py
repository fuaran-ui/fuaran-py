"""Published path of :mod:`fuaran_ui._core.function.registry` — re-exported unchanged.

The implementation moved behind the private Core boundary in Phase 1872 (see
:mod:`fuaran_ui._core`); this module keeps ``fuaran_ui.function`` importable with every
public name it has always carried.
"""

from __future__ import annotations

from ._core.function.registry import *  # noqa: F403
