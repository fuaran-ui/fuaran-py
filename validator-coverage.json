{
  "version": 1,
  "host": "fuaran-py",
  "family": "pre-emit",
  "vocabulary": "wire-format-fixtures/validator/defect-vocabulary.json",
  "posture": "subset",
  "postureReason": "A co-equal authoring + rendering tier, so every rule here is in principle in scope. The subset is historical, not designed, and it is a DIFFERENT subset from the TypeScript tier's — the divergence this phase exists to make visible. FUARAN069, named as the highest-value gap when this declaration was first written, is now implemented.",
  "implemented": [
    "FUARAN-DUP-ID",
    "FUARAN-EMPTY-ID",
    "FUARAN069",
    "FUARAN082",
    "FUARAN083",
    "FUARAN086",
    "FUARAN087",
    "FUARAN088",
    "FUARAN089",
    "FUARAN097",
    "FUARAN100",
    "FUARAN101",
    "FUARAN108"
  ],
  "abstentionDefault": "Not ported. This is an honest 'not yet', not a principled exclusion: the set above is whichever rules this host happened to need, and an unported rule is a gap.",
  "abstained": {
    "FUARAN099": "Not ported, and named here rather than left to the default because it was considered and declined with its two siblings. FUARAN100 and FUARAN101 ARE implemented: each is decidable from the field alone — a rule slot the control cannot honour, and a literal compare operand duplicating a bound the control already declares. FUARAN099 is not. Its second clause is 'and nothing in the tree writes it', which needs a tree-wide write-key projection and an opaque-writer stand-down this host does not have: with no writer projection a partial port would fire on every form whose compare reads a key some sibling handler writes, which is a false accusation at Error severity on an ordinary shape. Porting it means porting the reference's tree-wide state-key facts first, which is its own work.",
    "FUARAN105": "Not ported, and named here rather than left to the default because it was considered and declined. This host's pre-emit walker has no binding traversal at all — it descends child NODES and reads per-kind fields, never into a Binding's Transform source — and computes no tree-wide write-key or opacity projection. The rule reasons from the ABSENCE of a writer, so a partial port would not merely miss findings: with no writer projection it would fire on every tree that legitimately writes the key. Declined on the same reasoning, and for the same reason, as the TypeScript tier's abstention on this code."
  },
  "otherFamilies": {},
  "otherFamiliesNote": "Codes this host raises that belong to the reference's BUILD-TIME source-AST walker, not the pre-emit family enumerated in the vocabulary. Recorded so the gate reads them as a different family rather than as a code this host invented. That second family is not yet enumerated; until it is, this list is a declaration of intent, not something the gate can verify.",
  "machineChecked": true,
  "machineCheckedNote": "The FUARAN code is a first-class field on `Finding`, so `implemented` above is checked against what the validator actually raises: `tests/test_validator_coverage.py` fails on a code raised but not declared, a code declared but not raised, and a code this host invented that is absent from the canonical vocabulary. Recovered by source scan of `Finding(...)` constructor positions rather than by running the validator, because running it would only find the rules some fixture happens to trigger — and the rules no fixture reaches are precisely the ones a coverage check must not miss."
}
